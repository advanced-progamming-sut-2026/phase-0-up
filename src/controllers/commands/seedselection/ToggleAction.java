package controllers.commands.seedselection;

public enum ToggleAction {
    ADD,
    REMOVE,
    // Spend the Imitater on a second packet of another plant. The named plant is the one being COPIED,
    // not the Imitater itself.
    IMITATE,
}
